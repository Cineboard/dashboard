'use strict';

describe('Cineboard.categories module', function() {

    beforeEach(module('Cineboard.categories'));

    describe('Categories Controller', function() {

        it('should ...', inject(function ($controller) {
            //spec body
            var Ctrl = $controller('CategoriesController');
            expect(Ctrl).toBeDefined();
        }));

    });
});
