'use strict';

describe('Cineboard.libraries module', function() {

    beforeEach(module('Cineboard.libraries'));

    describe('Libraries Controller', function() {

        it('should ...', inject(function ($controller) {
            //spec body
            var Ctrl = $controller('LibrariesController');
            expect(Ctrl).toBeDefined();
        }));

    });
});
